FROM golang:1.10-alpine3.7
WORKDIR /go/src
RUN \
  set -ex; \
#  echo "http://alpine.gliderlabs.com/alpine/v$(cut -d'.' -f1,2 /etc/alpine-release)/main" > /etc/apk/repositories; \
  apk -U --no-cache add bzip2 git gcc build-base file upx; \
  go version; env; go env
RUN \
  set -ex; \
  go get -v github.com/robfig/glock; \
  go get -vd github.com/BurntSushi/toml; \
  go get -vd github.com/Microsoft/go-winio; \
  go get -vd github.com/docker/docker; \
  go get -vd github.com/docker/go-units; \
  go get -vd github.com/fsouza/go-dockerclient; \
  go get -vd github.com/gorilla/context; \
  go get -vd github.com/gorilla/mux; \
  go get -vd golang.org/x/net; \
  go get -vd golang.org/x/sys; \
